---
- name: Install Palette Insight
  hosts: insight-server
  remote_user: "{{ uservar }}"
  become: yes
  become_user: root

  roles:
    - common
    - palette-dev-tools
    - nginx
    - supervisor
    - splunkforwarder
    - golang
    - greenplum
    - insight-server
