---
- name: Install Palette Insight
  hosts: insight-server
  remote_user: vagrant
  # remote_user: user
  become: yes
  become_user: root

  roles:
    - common
    - palette-dev-tools
    - nginx
    - supervisor
    - splunkforwarder
    - golang
    - insight-server
